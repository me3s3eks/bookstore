package com.encom.bookstore.repositories;

import com.encom.bookstore.model.PaperBookProperties;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PaperBookPropertiesRepository extends JpaRepository<PaperBookProperties, Long> {

}
